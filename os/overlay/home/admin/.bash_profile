# KrdOS admin profile
# This file is sourced on login — the Flutter shell handles the UI,
# so there's nothing to auto-start here for the desktop session.
export PATH="/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$PATH"
export KRDOS_HOME="/opt/krdos"
export KRDOS_SHELL="1"
